# Testing

Manual testing

- Install on test device/emulator and validate reading, downloads, login, and playback.

Automated testing

- Widget tests: `flutter test` with `test/widget_test.dart` style tests.
- Integration: use `integration_test` package and run on device/emulator.

Release testing

- Publish to internal test track on Play Store and verify on a fleet of devices.
